--- a/webview/assets/local-conversation-thread-CGdGhhp8.js
+++ b/webview/assets/local-conversation-thread-CGdGhhp8.js
@@ -13463,6 +13463,10 @@
   (0, Q.useEffect)(() => {
     J?.id == null || J.readAt != null || q(J.id);
   }, [J?.id, J?.readAt, q]);
+  (0, Q.useEffect)(() => {
+    let e = typeof x === `string` ? x.trim() : ``;
+    e && (document.title = `${e} | Codex`);
+  }, [x]);
   let le = $e(),
     { agentMode: de } = Tn({ conversationId: e, hostId: C }),
     fe = S ? ze(S) : null,
